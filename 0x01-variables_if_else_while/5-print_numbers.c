#include <stdio.h>

/**
 * main - Entry point for the program
 *
 * Return: Always 0 (success)
 */

itn main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf('"\n");

	return (0);
}
